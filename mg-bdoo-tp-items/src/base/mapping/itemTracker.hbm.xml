<?xml version="1.0"?>
<!DOCTYPE hibernate-mapping PUBLIC 
"-//Hibernate/Hibernate Mapping DTD 3.0//EN"
"http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
  <class name="base.test.Contact" table="CONTACT">
   <id name="id" type="long" column="ID" >
   <generator class="native"/>
  </id>

  <property name="users">
   <column name="FIRSTNAME" />
  </property>
 </class>
</hibernate-mapping>

<!-- 

PROPERTYS
=========

<property name="propertyName"  El nombre de la propiedad.
		column="column_name" (opcional – default  nombre de la propiedad): El nombre de la columna a la que mapeará la propiedad.
		type="typename" (opcional): El nombre que indica el tipo a usar (básicos o custom)
		update="true|false" insert="true|false" (opcional – default true) : Especifica si la(s) columna(s) deben ser incluidas en las sentencia SQL UPDATE y/o INSERT.
		formula="SQL expression" (opcional): Una expresión SQL que define el valor para una propiedad calculada. 
		access="field|property|ClassName"  access (opcional – default property): La estrategia que debe usar hibernate para accesar la propiedad.
		lazy="true|false" (opcional – default false): Especifica si la propiedad debe ser cargada lazy cuando la variable de instancia es accesada por primera vez .
		unique="true|false" (opcional): Permite la generación de un restricción unique.  
		not-null="true|false" (optional): Permite la generación de una restricción not-null para la columna.
		generated="never|insert|always" (opcional – default never): Especifica si el valor de la propiedad es generada por la base de datos. 
	/>

Tipos PREDEFINIDOS BASICOS (como in Integer) ver filminas 47 y 47
=================================================================

Agregacion: COMPONENTES filmina 52
==================================
- Definiendo una clase como componente de otra, le indica a Hibernate que se persistirá en la misma tabla
- Un componente no define una clave primaria

<class name="Contrato">
  <id ../>
  <property name="nombre" not-null=“true”/>
  <component name="locador" class="Locador">
      <property name="dni" column="DNI" not-null="true" />
      <property name="nombre" column="NOMBRE_LOCADOR" not-null="true"/>
  </component>
</class>

Ó

<class name="Contrato">
  <id .../>
  <property name="nombre" not-null=“true”/>
  <component name="locador" class="Locador">
      <parent name=“contrato”/>             //// DIFERENCIA!!
      <property name="dni" column="DNI" not-null="true" />
      <property name="nombre" column="NOMBRE_LOCADOR" not-null="true"/>
  </component>
</class>

 -->